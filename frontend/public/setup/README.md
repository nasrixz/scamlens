# Setup page screenshots

Drop PNG/JPEG screenshots here. Pages reference them via `<img src="/setup/<file>" />`.

Recommended captures:

| File                       | Step it shows                                                |
|----------------------------|--------------------------------------------------------------|
| `android-private-dns.png`  | Android Settings → Network → Private DNS screen              |
| `ios-profile-install.png`  | iOS "Install Profile" prompt in Settings                     |
| `ios-device-mgmt.png`      | Settings → General → VPN & Device Management                 |
| `windows-dns-manual.png`   | Windows 11 "Edit DNS settings" dialog                        |
| `macos-dns-add.png`        | macOS System Settings → Network → Details → DNS              |
| `linux-nm-gui.png`         | GNOME NetworkManager GUI, IPv4 tab                           |
| `router-tplink-dns.png`    | TP-Link Advanced → Network → Internet screen                 |

Keep images under 400 KB — the dashboard auto-refresh is bandwidth-sensitive.
